<!DOCTYPE html>
<html>
<head>
  <title>Semifinali</title>
  <link rel="stylesheet" href="/style.css">
  <link
			href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css"
			rel="stylesheet"
		/>
    <meta name="viewport" content="width=device-width, initial-scale=1">

    <link rel="icon" type="image/x-icon" href="/favicon.ico">
</head>
<body class="p-4">

  <div class="mb-3">
    
  <h1>Semifinali 🫂</h1>
  <p>Inserisci i risultati delle semifinali</p>
  
  <h2>Round Gold</h2>
  <form action="/semifinali/risultati" method="POST">
    <% gold.forEach((match, index) => { %>
      <h4>Partita <%= index + 1 %></h4>
      <p><%= match.squadra1[0] %> + <%= match.squadra1[1] %> vs <%= match.squadra2[0] %> + <%= match.squadra2[1] %></p>
      <label>Squadra 1:</label>
      <input type="number" class="form-control-sm" name="gold<%= index %>a" required>
      <label>Squadra 2:</label>
      <input type="number" class="form-control-sm" name="gold<%= index %>b" required>
      <hr>
    <% }) %>

    <br><br>

    <h2>Round Silver</h2>
    <% silver.forEach((match, index) => { %>
      <h4>Partita <%= index + 1 %></h4>
      <p><%= match.squadra1[0] %> + <%= match.squadra1[1] %> vs <%= match.squadra2[0] %> + <%= match.squadra2[1] %></p>
      <label>Squadra 1:</label>
      <input type="number" class="form-control-sm" name="silver<%= index %>a" required>
      <label>Squadra 2:</label>
      <input type="number" class="form-control-sm" name="silver<%= index %>b" required>
      <hr>
    <% }) %>

    <button class="btn btn-primary" type="submit">Vai alle finali</button>
  </form>

  </div>
</body>
</html>
